{
  "version": 2,
  "builds": [
    {
      "src": "s3-upload-api.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/s3-upload-api.js"
    }
  ],
  "env": {
    "AWS_ACCESS_KEY_ID": "@aws_access_key_id",
    "AWS_SECRET_ACCESS_KEY": "@aws_secret_access_key",
    "AWS_REGION": "@aws_region",
    "S3_BUCKET_NAME": "@s3_bucket_name"
  }
}